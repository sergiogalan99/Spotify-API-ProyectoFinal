import { NoImageDosPipe } from './no-image-dos.pipe';

describe('NoImageDosPipe', () => {
  it('create an instance', () => {
    const pipe = new NoImageDosPipe();
    expect(pipe).toBeTruthy();
  });
});
